/** Aktor dla wszystkich zapisów z dashboardu (§4 tech-stack, plan Fazy 5 M1) — jedna, dzielona
 * tożsamość (auth per-hasło, NFR-1), spójna z `revisions.actor`/`audit_log.actor`. */
export const DASHBOARD_ACTOR = 'human-dashboard';
